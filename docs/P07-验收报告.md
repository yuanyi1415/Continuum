# P07｜Codex 原生适配闭环｜验收报告

## 当前状态

```text
IMPLEMENTED / AWAITING PRODUCTION VERIFY
```

---

## 本地自动验收

当前开发环境使用 Node 22 + development fallback，只用于实现期测试。

### P07 专项

```text
8 tests
8 passed
0 failed
```

覆盖：

1. explicit Work Intent parser；
2. ad-hoc Codex prompt 不绑定；
3. `implement P03` 自动绑定；
4. SessionStart resume；
5. Stop / SessionEnd reconcile 幂等；
6. DECISION Hook Gate；
7. MCP native-decision handoff；
8. deterministic BLOCK；
9. Session Suppression；
10. repo-local installer merge / idempotency；
11. Host capability detection；
12. 非 Continuum Repo Hook 完全 NOOP。

### 全量回归

```text
41 tests
41 passed
0 failed
```

P01～P06 回归全部通过。

---

## Production Verify

在 Node 24 + `better-sqlite3` + 真实 Codex 环境运行：

```bash
./scripts/verify-p07.sh
```

自动验证：

- 真实 `codex exec` Hook dispatch；
- ad-hoc 不绑定；
- explicit intent bind；
- Work Baseline 冻结；
- fresh Session resume；
- Stop / SessionEnd checkpoint；
- lost-hook recovery；
- Hook Gate fallback；
- BLOCK；
- capability detection；
- P01～P06 regression。

完成自动验收后，还必须完成一次真实 Codex GUI Structured Elicitation：

```bash
./scripts/verify-p07-codex-gui.sh
```

该脚本会启动 disposable fixture、注册临时 MCP Server，并要求用户在 Codex GUI 发送：

```text
continuum decision
```

真实选择后验证 pending Interaction 已被 resolve，并自动清理临时 MCP registration。

---

## Done 条件

P07 标记 Done 前：

- `verify-p07.sh` 必须 PASS；
- Codex GUI MCP Elicitation 必须在当前真实宿主完成一次确认；
- 不允许为了 MCP UX 绕过 Hook Gate / BLOCK correctness。


## v0.2 生产验收修正

首次真实验收发现 `implement P03; ...` 未绑定。根因不是 Codex Hook dispatch，而是 explicit intent parser 只允许 Ticket ID 后接空白或结束符。现已放宽为常见中英文分隔标点，并增加回归测试；验收脚本仍保留带分号的真实输入，不通过修改测试输入规避问题。

## MVP v1.0 最终状态

Codex CLI 与 GUI 路径均已完成用户本地验收。P07：**DONE**。
