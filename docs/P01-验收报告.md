# P01｜验收报告

## 结论

P01 已完成代码实现并通过当前执行环境的核心自动化验收。

## 已通过

- Architecture dependency rules；
- `continuum init`；
- Durable `.continuum/` state；
- Runtime SQLite migration；
- `.continuum-local/` gitignore；
- `continuum status --json`；
- minimal `continuum doctor`；
- malformed Durable State fail-closed；
- clone 后无旧 Runtime DB 仍能读取 Project State；
- local repository identity 不随 clone path 漂移；
- Continuum 自举：本仓库已由 P01 初始化为 Continuum Project。

## 自动化结果

```text
6 tests
6 passed
0 failed
```

CLI 真实链路：

```text
Git Repo
→ init
→ status
→ doctor
→ commit .continuum
→ clone
→ status
→ doctor
```

通过。

## 自举基线

P01 code baseline：

```text
f62ba480e5ebf4fe85981efde8d528c4b752c8da
```

Continuum project state commit：

```text
b18766385ddec8f5f8518f37f5147e66d4c43ba3
```

## 环境限制

当前执行容器是 Node.js 22，且无法从 npm registry 正常安装依赖，因此本地自动化通过显式 DEV fallback 验证 SQLite / YAML / Schema 逻辑。

正式实现依赖仍然冻结为：

- Node.js 24 LTS；
- `yaml`；
- `ajv` / `ajv-formats`；
- `better-sqlite3`。

仓库提供：

```bash
./scripts/verify-p01.sh
```

用于在 Node 24 + 正式 npm dependencies 环境中完成 production-path 验收；脚本会额外断言 Runtime Driver 必须是 `better-sqlite3`。
